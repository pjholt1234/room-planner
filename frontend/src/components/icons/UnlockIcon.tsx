const UnlockIcon = () => {
    return (
        <span
            className="material-symbols-outlined"
            style={{ transform: 'rotate(15deg)' }}
        >
            lock_open
        </span>
    );
};

export default UnlockIcon;
